{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:s3": "npm run build && aws s3 sync dist/ s3://ovosur-planta1-moraytech --delete"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "chart.js": "^4.4.9",
    "chartjs-plugin-zoom": "^2.2.0"
  }
}
